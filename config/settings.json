{
  "permissions": {
    "defaultMode": "auto"
  },
  "hooks": {
    "ConfigChange": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash $HOME/.claude/hooks/sync-config-repo.sh 2>/dev/null || true",
            "statusMessage": "Sincronizando config/ com claude-code-setup..."
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash $HOME/.claude/hooks/sync-config-repo.sh 2>/dev/null || true",
            "statusMessage": "Verificando sincronização de config/packages..."
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash $HOME/.claude/hooks/tts-response.sh 2>/dev/null || true",
            "statusMessage": "Lendo resposta em voz alta...",
            "async": true
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash $HOME/.claude/hooks/sync-memory-repo.sh 2>/dev/null || true",
            "statusMessage": "Sincronizando memória com claude-code-setup..."
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash $HOME/.claude/hooks/sync-config-repo.sh 2>/dev/null || true",
            "statusMessage": "Verificando pacotes instalados..."
          }
        ]
      }
    ]
  },
  "language": "pt-BR",
  "voice": {
    "enabled": false,
    "mode": "hold"
  },
  "theme": "auto",
  "autoMode": {
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: assume private unless the remote host and repo name indicate otherwise, or a visibility check in the transcript shows public",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: None configured",
      "**Default / protected branches**: unknown — this directory has no git remotes and origin/HEAD is unset",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Source control**: No git remotes configured in this directory (~/home/adriano is not itself a repo with remotes) — no trusted source control scope established",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar; note .claude/.credentials.json and browser profile secrets paths were seen in a config scan and should be treated as sensitive",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment (hyphen/underscore/dot-delimited — e.g. matches `prod-db`, not `producer`)",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: software development",
      "**Trusted repo**: No git repository detected at this working directory (/home/adriano) — no remotes configured; treat as having no established trusted repo scope until run inside an actual project checkout",
      "**Org-specific CLIs**: None configured",
      "routine under adriano/ prefix: not established — no repo/org context to scope this qualifier"
    ]
  },
  "voiceEnabled": false
}
